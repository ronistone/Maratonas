#include <bits/stdc++.h>
using namespace std;
vector<ll>v;

void sieve(bool **prime,int n){
	
	memset(prime, true,sizeof(prime));
	v.clear();
	for(i=n;i<=10000 or v.size()<10;i++){
		if(prime[i]==true){
			v.emplace_back(i);
		


main(){
	int n;
	cin >> n;
	bool prime[10000];
	sieve(&prime,n);
}

#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;
typedef pair<char,ll> ci;

main(){
	ll n,x;
	char a;
	vector<ci> d;
	while(cin >> n and n){
		cin.ignore();
		for(int i=0;i<n;i++){
			cin >> a >> x;
			d.push_back(make_pair(a,x));
		}
	}
	
	
}
